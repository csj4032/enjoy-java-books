dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile "org.immutables:value:2.8.2" // for annotations
    compile "org.immutables:builder:2.8.2" // for annotations
    compile "org.immutables:gson:2.8.2" // for annotations
}